# 公私钥配置

## 1、Github SSH key

```
# 首先查看是否存在key
ls ~/.ssh
# 创建公钥
ssh-genkey -t rsa -C "123@123.com"
# 复制公钥
cat ~/.ssh/id_rsa.pub

# 然后在github上添加SSH key，将公钥复制进去即可，然后下面验证
ssh -T git@github.com
```



## 2、服务器 SSH key